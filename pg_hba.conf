# PostgreSQL Host-Based Authentication Configuration
# Configured for TCP connections from localhost for test suite

# Enable host-based authentication (allows TCP connections)
host all all all md5

# Keep local Unix socket authentication (for internal operations)
local all all md5
